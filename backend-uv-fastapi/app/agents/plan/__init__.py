"""规划子包：`FinalRequirement` → `FilePlan`（阶段 5 实现，阶段 6 入图）。

规划为什么必须是**独立节点**而不是并进生成循环（docs/agent_refactor_plan.md §3.2.1）：

- 完成门禁需要一份**事先声明**的清单才能判定"交付完整"；并进循环后就只能靠模型自称完成；
- 难度 → 步数/预算的映射必须在**进入循环之前**确定，规划错了可以立刻失败，不必等整个循环跑完。
"""
