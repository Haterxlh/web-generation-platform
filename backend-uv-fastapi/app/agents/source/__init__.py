"""文档源子包：解析后的"理解"与角色裁决（阶段 3）。

三块职责，刻意分开：

- ``role_policy``：**纯函数**，按"解析器能力"裁决附件的角色（判定权归模型、否决权归 Python）——
  无 IO、无模型，可整块离线单测；
- ``doc_digest_agent``（本阶段）：文件 → ``RequirementDigest``，唯一在这里花 token 的地方；
- 解析本身（无 LLM）在 ``app/utils/doc/``，不放在 agent 子包里 ——
  它连"模型"这个概念都不需要知道，放进来只会让人误以为解析也要调模型。
"""
