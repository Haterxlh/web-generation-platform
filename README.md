# Web Generation Platform

基于 Spring Boot 4.0.8 + JDK 21 的 AI 代码生成平台。

## 技术栈
- Spring Boot 4.0.8
- JDK 21
- Maven
- MySQL
- Redis
- LangChain4j

## 快速启动
1. 确保 JDK 21 已安装
2. 执行 `mvn clean install`
3. 运行 `mvn spring-boot:run`
4. 访问 `http://localhost:8080`

## 数据库初始化
参见 `sql/` 目录下的脚本。